{
  "name": "react-artifact",
  "private": true,
  "author": "Julian Low",
  "license": "MIT",
  "hoist": true,
  "stream": true,
  "workspaces": [
    "packages/*",
    "examples/*"
  ],
  "scripts": {
    "build": "npx lerna run build",
    "test": "jest --config config/jest.config.js",
    "test-changed": "test -o",
    "test-watch": "test --watchAll",
    "coverage": ""
  },
  "peerDependencies": {
    "react": "16.8.6",
    "react-dom": "16.8.6"
  },
  "devDependencies": {
    "@types/jest": "^26.0.9",
    "jest": "^26.3.0",
    "lerna": "^3.22.1"
  }
}
